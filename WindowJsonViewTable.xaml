<Window x:Class="KLCProxy.WindowJsonViewTable"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:KLCProxy"
        mc:Ignorable="d"
        Title="WindowJsonViewTable" Height="560" Width="550">
    <Grid>
        <TabControl>
            <TabItem Header="Table">
                <DataGrid x:Name="dataGrid" ItemsSource="{Binding DTsource, Mode=OneWay}" IsReadOnly="True" AutoGenerateColumns="False" HorizontalScrollBarVisibility="Auto" SelectionMode="Single" SelectionUnit="Cell" ClipboardCopyMode="None" PreviewKeyDown="DataGrid_PreviewKeyDown">
                    <DataGrid.Columns>
                        <DataGridTextColumn Binding="{Binding Key}" Header="Key"/>
                        <DataGridTextColumn Binding="{Binding Value}" Header="Value"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
            <TabItem Header="JSON">
                <TextBox Margin="0" Grid.Row="1" TextWrapping="Wrap" Text="{Binding TXTsource, FallbackValue=JSON, Mode=OneWay}" Grid.ColumnSpan="2" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"/>
            </TabItem>
        </TabControl>
    </Grid>
</Window>