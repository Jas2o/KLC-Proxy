<Window x:Class="KLCProxy.WindowGenericEntry"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:KLCProxy"
        mc:Ignorable="d"
        Title="Generic Entry" Width="300" SizeToContent="Height" WindowStyle="ToolWindow" WindowStartupLocation="CenterOwner" Loaded="Window_Loaded">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Label x:Name="lblLabel" Content="Optional label" />
        <TextBox x:Name="txtValue" Grid.Row="1" Margin="5" VerticalAlignment="Center" KeyDown="txtName_KeyDown" />
        <Button x:Name="btnSave" Content="OK" Grid.Row="2" Margin="5" Click="btnSave_Click" HorizontalAlignment="Right" Width="80" />
    </Grid>
</Window>
